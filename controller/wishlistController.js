const Product = require("../model/productModel");
const User = require("../model/userModel");
const Wishlist = require("../model/wishlistModel");

const wishlistpost = (req , res) => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

module.exports = {
    wishlistpost
}